name: json_serializable_plus
description: A dart package to detect serializable for generate a class model
version: 1.1.0
homepage: https://github.com/irwan75/json_serializable_plus.git

publish_to: none

environment:
  # sdk: '>=2.17.0 <3.0.0'
  sdk: ^3.9.2

dependencies:
#   path: ^1.8.0
  # build: ">=2.3.1 <2.4.2"
  build: ^4.0.0
  json_annotation_plus:
    git: 
      url: https://github.com/irwan75/json_annotation_plus.git
      ref: 2.0.0
  # json_annotation_plus:
  #   path: ../json_annotation_plus  
  # source_gen: ">=1.2.7 <1.6.0"
  source_gen: ^4.0.1

# dev_dependencies:
  # lints: ^2.0.0
  # test: ^1.16.0

topics:
  - json-annotation-plus
  - source-gen
  - build